# 1. yarn eject

react 프로젝트를 실행하기위해 yarn start를 사용했었다.

yarn eject라는 실행 명령어도 있었는데 eject 가 담당하고 있는 역할은 생성된 리액트 파일안에 숨겨져 있던 툴들이 숨겨져 있다.

이것을 열어서 볼 수 있는 방법 중 하나인데 eject를 사용하면 숨겨진 툴들을 다시 모으지 못한다.

대표적인 숨겨진 툴안에 이러한 것들이 있다.

Babel , WebPack , ESLint , JEST , POST CSS

# 2. 숨겨진 툴

## 2-1. Babel 이란?

> Babel 공식 웹사이트 정의<br>
> Babel is a toolchain that is mainly used to convert ECMAScript 2015+ code into a backwards compatible version of JavaScript in current and older browsers or environments. <br>
> 바벨은 주로 ECMAScript 2015+ 코드를 현재 및 과거의 브라우저와 같은 환경에서 호환되는 버전으로 변환하는데 주로 사용되는 도구입니다.

바벨은 크로스 브라우징 이슈를 해결하기 위해 생겨난 툴로 다른 브라우저 환경에서 실행되면 코드의 일관성이 망가뜨려져 이 부분을 해결하기 위해서 만들어졌다.

바벨은 자바스크립트의 컴파일러로써 ES6+의 자바스크립트 코드는 하위 버전으로 변환되고, 그로 인해 IE나 다른 브라우저에서 동작할 수 있게 만든다.

## 2-2 Babel 의 빌드

1. 파싱(Parsing) : 코드를 읽고 추상 구문 트리(AST)로 변환하는 단계
2. 변환(Transforming) : 추상 구문 트리를 변경
3. 출력(Printing) : 변경된 결과물을 출력

바벨은 3단계 순서로 변환이 되는데 **파싱**과 **출력**만 바벨이 담당하고 변환은 **플러그인**이 맡게된다. 플러그인은 바벨이 어떤 코드를 어떻게 변환할 지에 대한 규칙을 나타내고 플러그인들을 목적에 따라 묶어놓은 세트를 프리셋(preset)이라고 한다.

> 추상 구문 트리란?<br>
> 추상 구문 트리란 프로그램 내에서 발생하는 기능을 나타내기 위해 만들어진 구문 구조이다. <br>
> 고수준의 언어를 기계어로 변환하는 과정에서 꼭 필요하다. 코드를 트리 구조의 데이터 스트럭쳐로 만들어내게 되는데 이렇게 구조화시키면 프로그래머도, 컴퓨터도, 훨씬 파악하기 쉬워진다.

## 2-3. 폴리필 (polyfill)

폴리필은 최신 ECMAScript 환경을 만들기 위해 코드가 실행되는 환경에 존재하지 않는 빌트인, 메소드 등을 추가하는 역할을 한다.

# 3. Webpack

<img src='https://nesoy.github.io/assets/logo/webpack.png'/>

## 3-1. Webpack 이란?

- 현대 Javascript Application의 Static Module Bundler 이다.
- html 파일에 들어가는 자바스크립트 파일들을 하나의 자바스크립트 파일로 만들어주는 방식을 Module Bundler 라고 한다.

## 3-2. Wepack 사용 이유

- 옛날에는 페이지마다 새로운 html을 요청해서뿌려 주는 방식이였다면, 최근에는 SPA 하나의 html 페이지에 여러개의 자바스크립트 파일들이 포함한다. 연관 되어 있는 자바스크립트 종송석 있는 파일들을 하나의 파일로 묶어서 관리할 수 있다.
- 파일을 컴파일 할 때, 여러 모듈들의 파일을 읽어오는데 시간이 오래 걸린다. 그 부분을 해결하기 위해 여러 파일을 하나의 파일로 번들링 해준다.
- 하나의 자바스크립트 파일로 만들어서 웹페이지 성능 최적화 해준다.

# 4. ESLint

## 4-1. ESLint 란?

ESLint는 ES 와 Lint를 합친 것으로

ES는 Ecma Script로서, Ecma라는 기구에서 만든 Script, 즉, 표준 Javascript를 의미한다.
Lint는 에러가 있는 코드에 표시를 달아놓는 것을 의미한다.

따라서, ESLint는 자바스크립트 문법에서 에러를 표시해주는 도구이다.
코드의 에러나, 정말 문제가 되는 부분만을 지정할 수도 있고, 아니면 전반적인 코딩스타일(ex. tab 설정, ; 여부 등)까지 지정할 수도 있다.

# 5. Jest

## 5-1. Jest 란?

Jest 는 코드가 제대로 동작하는 지 확인하는 test case 를 만드는 '테스팅 프레임워크' 이다.

# 6. PostCSS

## 6-1. PostCSS

POST CSS는 우리의 CSS를 조금더 현대적으로 바꿔주는 플러그인이다.

좀더 풀어 설명하자면 POST CSS 는 JS 플러그인을 사용하여 CSS를 변환시키는 툴 입니다.

POST CSS 는 언어가아니라 자동으로 신기술 CSS 를 호환가능하도록 변환시켜주는 플러그인일 뿐이다.

CSS에 문제가없는지 미리 확인해서 에러로그를 준다.

지금 발전중인 CSS의 현대기술들을 브라우저에 호환되도록 자동 변환해준다.

PostCSS 자체는 아무 일도 하지 않는다. 다만 다양한 플러그인과, 플러그인을 추가할 수 있는 환경을 제공할 뿐이다.
